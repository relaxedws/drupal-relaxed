resources:
  relaxed:root:db:
    HEAD:
      supported_formats:
        - json
      supported_auth:
        - cookie
    GET:
      supported_formats:
        - json
      supported_auth:
        - cookie
    PUT:
      supported_formats:
        - json
      supported_auth:
        - cookie
    POST:
      supported_formats:
        - json
      supported_auth:
        - cookie
    DELETE:
      supported_formats:
        - json
      supported_auth:
        - cookie
  relaxed:root:default:doc:
    HEAD:
      supported_formats:
        - json
      supported_auth:
        - cookie
    GET:
      supported_formats:
        - json
      supported_auth:
        - cookie
    PUT:
      supported_formats:
        - json
      supported_auth:
        - cookie
    DELETE:
      supported_formats:
        - json
      supported_auth:
        - cookie
  relaxed:bulk_docs:default:
    POST:
      supported_formats:
        - json
      supported_auth:
        - cookie
  relaxed:revs_diff:default:
    POST:
      supported_formats:
        - json
      supported_auth:
        - cookie
