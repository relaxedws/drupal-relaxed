id: relaxed.ensure_full_commit
plugin_id: 'relaxed:ensure_full_commit'
granularity: resource
configuration:
  methods:
    - POST
  formats:
    - json
  authentication:
    - basic_auth
    - cookie
dependencies:
  module:
    - basic_auth
